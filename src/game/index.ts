/**
 * Game entry: re-export config and public API.
 * Import from here for a single entry point (e.g. main.ts).
 */
export { gameConfig } from './config';
export type { MaskLayerEntry } from './masks/MaskSystem';
export { MaskType } from './masks/MaskType';
