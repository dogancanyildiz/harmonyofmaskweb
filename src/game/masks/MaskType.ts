/**
 * Mask types: each corresponds to one tile layer (green/red).
 * Only the active mask's layer is visible; collision is always on for all layers.
 */
export enum MaskType {
  Green = 'green',
  Red = 'red',
}
